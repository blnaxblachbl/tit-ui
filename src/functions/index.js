import { normalize } from './normalize'
import hexToRgba from './hexToRgba'
import {
    wordsFromUpperCase,
} from './regexes'
import measure from './measure'

export default {
    normalize,
    hexToRgba,
    wordsFromUpperCase,
    measure
}