import { normalize } from './normalize'
import hexToRgba from './hexToRgba'
import {
    wordsFromUpperCase,
} from './regexes'

export default {
    normalize,
    hexToRgba,
    wordsFromUpperCase
}